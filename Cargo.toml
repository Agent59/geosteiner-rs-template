[package]
name = "geosteiner-rust-api"
version = "0.1.0"
edition = "2021"
build = "build.rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
cty = "0.2.2"

[features]
rebuild_c = [] # feauture has no dependencies
