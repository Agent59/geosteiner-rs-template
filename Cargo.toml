[package]
name = "geosteiner-rs-template"
version = "0.1.0"
edition = "2021"
build = "build.rs"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]

[features]
rebuild_c = [] # feauture has no dependencies
